# 粤语声调台 · canto-learn

杨万的手机粤语学习网页。2026-09-15 从旧卡片站重构为“课程原句 → 逐字粤拼 → 单字音频 → 整句/整组音频”的长期课程底座；当前正式内容只发布第一课发音基础。

## 当前第一课

1. 音节结构：声母 onset + 韵母 final + 声调 tone。
2. LSHK 粤拼 1–6 与六个相对音高轮廓。
3. 普通话母语者易混对：1↔3、3↔6、4↔6、2↔5、5↔6。
4. 入声韵尾 checked endings：-p / -t / -k。
5. `aa/a`、`ng-`、成音节鼻音、`-m/-n/-ng`、`oe/eo`、`z/c/s`、`j/gw/kw` 的第一遍辨认。
6. 10 题随机听辨基线：覆盖 `si`、`fan` 与数字三组样音；成绩只存本机，不设任意及格线。

移动端每个字都是独立按钮，并用 `<ruby><rt>` 显示具体语境中的粤拼；整组另设播放键。播放器一次只播放一段音频，支持 0.75× / 1× / 1.25× 和 1× / 3×。网页可安装为 PWA，访问过的音频按需离线缓存。

## 数据与音频边界

- `course.js` 是网页唯一课程数据源；不再分别维护 `cards.js` 与 `lessons.js` 两份内容。
- 后续课堂句子使用 `sentenceAudio + tokens[{char, contextualJyutping, charAudio}]`，逐字记录语境读音；标点不设音频。
- 单字练习音与自然整句录音是两种资产，禁止把单字文件机械拼成“整句”。
- 现有音频是 Microsoft `zh-HK-HiuMaanNeural` 生成的练习样音，已检查文件可解码，但不等于老师或母语者人工纠音。
- 标音参考 [LSHK Jyutping](https://jyutping.org/jyutping/)、[粵典 words.hk](https://words.hk/) 与 [中大粤语审音配词字库](https://humanum.arts.cuhk.edu.hk/Lexis/lexi-can/)；多音字和变调仍须按课堂语境逐项复核。

## 文件地图

- `index.html`：语义结构与课程章节
- `styles.css`：移动优先界面
- `course.js`：课程、音节 token 与小测数据 SSOT
- `app.js`：渲染、播放器、小测与本机进度
- `sw.js` / `manifest.webmanifest`：安装与离线支持
- `audio/`：练习音频；保留旧课音频以便后续逐课迁移

旧版内容可从 Git 提交 `f3a5f05` 回看，不另存重复备份文件。

## 2026-09-15 发布验收

- 课程数据共引用 45 个音频键；仓库 120 个 MP3 均通过 macOS 解码检查，网页引用无缺失。
- 在 320px、390px 手机宽度和 1440px 桌面宽度实测：无横向溢出；逐字、整组、A/B 连播、倍速、重复与 10 题小测可用。
- 验证快速连续点音不会误报；播放器不会遮挡小测反馈或页尾链接。
- PWA fresh install、断网打开、已访问音频离线播放均通过；未缓存音频在断网时会明确提示先联网播放一次。
